services:
  app:
    build: .
    ports:
      - "1010:1010"
    develop:
      watch:
        - action: sync
          path: .
          target: /code
          ignore:
            - node_modules/
        - action: rebuild
          path: package.json