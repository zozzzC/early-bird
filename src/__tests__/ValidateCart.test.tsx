describe("validate cart function", () => {
  test.todo(
    "given an invalid cart with wrong prices for all options and base prices, the cart is updated, the item total is updated, and priceChanged is true and optionsChanged is false."
  );
  test.todo(
    "given an invalid cart with a milk option that cannot be found, and an extra option that cannot be found, the cart is updated, the item total is updated, and priceChanged and optionsChanged is true."
  );
  test.todo(
    "given a valid cart with correct prices for all options and base prices, the cart stays the same, and priceChanged and optionsChanged is false."
  );
});
