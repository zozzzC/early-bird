import { X } from "lucide-react";

export default function DeleteButton() {
  return (
    <div className="flex items-center">
      <button onClick={() => {}}>
        <X size={20}></X>
      </button>
    </div>
  );
}
